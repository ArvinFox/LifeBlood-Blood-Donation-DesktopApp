name: blood_donation_app
description: "A desktop application for managing blood donors and requests for the LifeBlood App."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  file_picker: ^9.0.2

  firebase_auth: ^5.5.2
  firebase_core: ^3.13.0

  provider: ^6.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
    
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/icons/
